# 模型目录说明

平台当前正式模型命名为 `yolo_hrca_seg`。

系统内置的版本关系：

- `yolo_hrca_seg_baseline`：基线对照版，用于回归比较。
- `yolo_hrca_seg_lite`：轻量复用版，用于边缘设备验证。
- `yolo_hrca_seg`：当前生产版，综合精度和推理速度最优。

真实模型文件请通过平台“模型管理 → 上传新模型”导入。上传后的文件保存在运行时生成的 `data/models/` 目录，并进入测试、灰度切换和生产复用流程。
